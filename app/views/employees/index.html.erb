<p id="notice"><%= notice %></p>

<h1>Employees</h1>

<table>
  <thead>
    <tr>
      <th>Rkactive</th>
      <th>Rkempcode</th>
      <th>Rkempname</th>
      <th>Rkempgender</th>
      <th>Department</th>
      <th>Rkhiredate</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @employees.each do |employee| %>
      <tr>
        <td><%= employee.rkactive %></td>
        <td><%= employee.rkempcode %></td>
        <td><%= employee.rkempname %></td>
        <td><%= employee.rkempgender %></td>
        <td><%= employee.department %></td>
        <td><%= employee.rkhiredate %></td>
        <td><%= link_to 'Show', employee %></td>
        <td><%= link_to 'Edit', edit_employee_path(employee) %></td>
        <td><%= link_to 'Destroy', employee, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Employee', new_employee_path %>
